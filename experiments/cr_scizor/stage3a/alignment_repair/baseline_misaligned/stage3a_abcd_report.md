# Preserved misaligned Stage 3A A-D baseline

The A-D structural audit passed, but the simulator transition-alignment smoke failed (median next-state L2 `3.50334445387125`, required `< 1e-4`). The original oracle outputs are therefore retained only as a `MISALIGNED_PROVISIONAL` baseline and are not usable to train the verifier.
