# Stage 3 v0.2-R Final Snapshot

Status: final validation-only delivery.

Completed: 3E-A through 3E-E, validation learning and protocol freeze, and 3H validation-only finalization.

Validation gate: `false`.

Final decision: `SWITCH_TO_COVERAGE_CONSTRAINED_SOFT_SCIZOR`.

The validation oracle contains 1,867 query groups and 7,468 replacement rows. Engineering rates for branch equality, reference replay, and finite targets are all `1.0`.

The final blind benchmark was not generated because the validation gate failed, as required by the protocol.

Large artifacts are catalogued in `large_artifact_manifest.json` and intentionally excluded from the lightweight ZIP.
